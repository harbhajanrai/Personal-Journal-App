There is no need of third party modules to be installed

To run the application, please follow following steps:
    -Unzip the folder
    -run the user.py file, application will then ask user to login or register.